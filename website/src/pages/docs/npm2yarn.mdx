# Npm2Yarn

export const Tabs = 0
export const Tab = 1

```sh filename="without npm2yarn metadata"
yarn add foo
```

```sh npm2yarn
npm i -D @graphql-eslint/eslint-plugin
```

```sh npm2yarn filename="Terminal"
npm i bar
```

````mdx filename="Markdown" /npm2yarn/
```sh npm2yarn
npm i bar
```
````
