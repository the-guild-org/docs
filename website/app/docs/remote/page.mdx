---
sidebarTitle: Remote MDX
---

import { RemoteContent, Tabs } from '@theguild/components'
import { defaultNextraOptions } from '@theguild/components/next.config'
import { compileMdx, fetchPackageInfo } from '@theguild/components/server'

export async function PackageApiDocs() {
  const PACKAGE_NAME = '@envelop/core'
  const { readme } = await fetchPackageInfo(PACKAGE_NAME)
  const { result } = await compileMdx(
    `
\`\`\`sh npm2yarn
npm i ${PACKAGE_NAME}
\`\`\`
${readme}`,
    defaultNextraOptions
  )
  return <RemoteContent compiledSource={result} components={{ $Tabs: Tabs }} />
}

# Remote MDX Support

<PackageApiDocs />
