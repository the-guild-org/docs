---
'@theguild/components': patch
---

enhance `withGuildDocs`, add `nextConfig.basePath`, `nextConfig.webpack` with `applyUnderscoreRedirects`, `images.unoptimized = true` if `basePath` was specified
